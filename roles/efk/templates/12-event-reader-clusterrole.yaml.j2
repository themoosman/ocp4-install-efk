apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: event-reader
  namespace: openshift-logging
rules:
- apiGroups: [""]
  resources: ["events"]
  verbs: ["get", "watch", "list"]
